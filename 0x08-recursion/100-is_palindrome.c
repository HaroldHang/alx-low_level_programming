#include <stdio.h>
#include <math.h>
char rev(char *str);
/**
 *is_palindrome - check if a palindrome
 *@s: the string
 * Return: Always 0 (Success)
 */
int is_palindrome(char *s)
{

}

/**
 *rev - performs the reverse string
 *@str: the string
 *@start: the start number
 * Return: Always 0 (Success)
 */
char rev(char *s)
{
char
}

